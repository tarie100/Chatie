import { createChatBotMessage } from 'react-chatbot-kit';
//import ChatieIcon from './chatie.jpg'
const config = {
  initialMessages: [createChatBotMessage("Hi, This is Chatie. How can I help you?")],
};

export default config;
